<!-- Written by the SessionEnd hook (~/.claude/hooks/surface-state.py). Latest update per surface; a new one rewrites its section. Edit by hand only to correct it. -->

# State — what was last touched, per surface

One entry per surface, holding only the most recent session that changed it.
This is not a changelog (`git log` is) and not a plan (each surface doc's
*Next* is) — it answers "where did work last land, and when".

## Other

**2026-09-07 20:15** — the panel now groups by repository (M3 shipped) — repos with a waiting session first, worktrees folded back into their parent repo, s to flip to status grouping — merged to origin/development @ 046b4…

`1c62803` · `046b4b3` · `8cd051b` · 10 files · branch `worktree-new-tab`
